<%= simple_form_for(@share_dream) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :tittle %>
      <%= f.input :description, as: :summernote, input_html: { id: 'body' } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
